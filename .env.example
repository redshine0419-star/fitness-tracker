# 사이트 URL — 배포 도메인이 정해지면 채운다 (canonical, OG, JSON-LD @id에 사용)
NEXT_PUBLIC_SITE_URL=https://example.org

# GTM 컨테이너 ID — PROJECT_SPEC §2-6: 허용되는 서드파티 스크립트는 GTM 1개뿐.
# 비어 있으면 GTM 자체를 로드하지 않는다 (Step 6).
NEXT_PUBLIC_GTM_ID=
